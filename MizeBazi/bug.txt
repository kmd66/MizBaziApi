

افزودن ابزار
dotnet tool install --global dotnet-ef --version 8.*

dotnet ef migrations add [Migration Name] --context OrgContexts
dotnet ef database update --context OrgContexts

--------------------------------

درست کردن AllowAllOrigins

ارسال و محدود کردن ارسال sms

محدود کردن دیوایس

محدود سازی کاربر
